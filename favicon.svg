<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Base layer: large muted circle (low priority) -->
  <circle cx="12" cy="20" r="10" fill="#9E9E9E" opacity="0.5"/>
  <!-- Mid layer: medium desaturated circle -->
  <circle cx="18" cy="18" r="8" fill="#78909C" opacity="0.7"/>
  <!-- Top layer: small vivid accent circle (highest priority) -->
  <circle cx="20" cy="12" r="6" fill="#FF5722"/>
</svg>
